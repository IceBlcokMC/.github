# IceBlockMC

> Note: The GitHub organization login is historically misspelled as `IceBlcokMC`.
> The correct display name is `IceBlockMC`.
> The correctly-spelled organization `IceBlockMC` is registered only to prevent name squatting.
> All repositories and dependencies live under `github.com/IceBlcokMC`.
